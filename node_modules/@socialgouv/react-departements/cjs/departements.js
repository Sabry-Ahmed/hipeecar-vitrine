"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hasPetiteCouronne = exports.normalizeDpt = void 0;

var normalizeDpt = function normalizeDpt(dpt) {
  var dtpNumber = Number(dpt);

  if (isNaN(dtpNumber)) {
    return dpt.toUpperCase(); // 2A, 2B
  }

  if (dtpNumber < 10) {
    return "0" + dpt;
  }

  return dtpNumber;
};

exports.normalizeDpt = normalizeDpt;

var hasPetiteCouronne = function hasPetiteCouronne(departements) {
  return ["75", "92", "93", "94"].filter(function (dpt) {
    return departements.map(normalizeDpt).includes(normalizeDpt(dpt));
  }).length === 4;
};

exports.hasPetiteCouronne = hasPetiteCouronne;